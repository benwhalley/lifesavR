- The **pipe** passes data from one piece of code to another
- The pipe looks like this: `%>%`
- Press `Cmd` + `Shift` + `M` to add a pipe.

- A **pipeline** is a list of functions joined by pipes.
- Store the output of a pipeline in a variable (using assignment, `<-`).