- `read_csv("http://...")` can load data from a URL.
- It converts the file to a `data.frame`.
- You must *assign* the loaded data to a *variable*  (give it a descriptive name).
- Use the **Environment** pane to view the data you loaded
